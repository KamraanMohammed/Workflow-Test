name: Workflow Test.

on:
  push:
    branches:
      - main

jobs:
  Workflow-Test-Job:
    runs-on: ubuntu-latest
    steps:
      - name: print Workflow test working.
        run: echo "Workflow testing working"
      - name: Sleep for 20 seconds.
        run: sleep 20
      - name: second line
        run: echo "second line of text"
